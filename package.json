{
  "scripts": {
    "dev": "nodemon index.ts",
    "docker:up": "docker-compose -f docker-compose.yml --project-name api up -d",
    "docker:down": "docker-compose -f docker-compose.yml --project-name api down -d",
    "docker:uninstall": "docker-compose -f docker-compose.yml --project-name api down --rmi all"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.22.0",
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.0.4"
  }
}
